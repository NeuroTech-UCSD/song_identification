

% Program constants
datafile = 'ImageryData.mat';
nfolds = 1;
ntestfolds = 1;
tmin = 1;
tmax = 1;
lambda = 1;

% Load data
load(datafile, 'eeg', 'stim', 'downFs');

% Generate training sets

% Cross validation

% Train model

% Generate plots